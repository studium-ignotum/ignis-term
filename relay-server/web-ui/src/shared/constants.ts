/**
 * Shared configuration constants for WebSocket relay
 */

// Terminal configuration
export const TERMINAL_MIN_COLS = 20;
export const TERMINAL_MIN_ROWS = 5;
export const TERMINAL_DEFAULT_SCROLLBACK = 50000;
